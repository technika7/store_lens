import { NextResponse } from 'next/server'; import fs from 'fs'; export async function POST(req: any) { const body = await req.json(); fs.writeFileSync('client-messages-debug.json', JSON.stringify(body, null, 2)); return NextResponse.json({ ok: true }); }
